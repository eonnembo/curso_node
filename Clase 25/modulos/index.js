const series = require('./series'); //Modulos propios
const fs = require('fs');           //Modulo nativo para archivos
const express = require('express'); // Modulos de terceros
console.log(series);
// console.log(express);